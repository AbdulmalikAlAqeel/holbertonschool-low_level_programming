*****************

Task0 : Function that allocates memory using `malloc`. Exits with status 98 on failure.
Task1 : Function that concatenates two strings with a limit on the second string.
Task2 : Function that allocates memory for an array using `malloc` and initializes it to zero.
Task3 : Creates an array of integers containing a range from `min` to `max`.
Task4 : Reallocates a memory block, handling data transfer and old block cleanup.
Rask5 : This project contains a C program that multiplies two arbitrarily large positive numbers. Standard integer types like int or long long have limits (overflow), so this program treats numbers as strings and performs "Long Multiplication" digit by digit, storing the results in dynamically allocated memory.
